1.Define what a function is in your own words?a peice of code that does a specific job.
2. What key word is used to define a function?def
3. What does a return statement do?return gives the answer from a function back to the program so it can be used.

4. What is the difference between arguments and parameters? parameters are the name listed when you create a function they like placeholders for imformation the function will receive.arguments are the actual values you give to the function when you call it.
